import '@testing-library/jest-dom'
import { getImagen } from '../../base/base-pruebas/11-async-await'


describe('Pruebas en 11-async-await.js', () => {

 /*    test('Prueba en getImagen() debe retornar un url (done)', ( done ) => {
        getImagen().then(url => {
            expect(url.includes('https')).toEqual(true)
            done();

        })

    })

    test('Prueba 2 en getImagen() debe retornar un url (async)', async (  ) => {
        
        const url = await getImagen();

        expect(url.includes('https')).toEqual(true)
    }) */

    test('should ', () => {
        
    })
    
    
})
